require 'spec_helper'

describe CalculateAccount do
  it 'has a version number' do
    expect(CalculateAccount::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
